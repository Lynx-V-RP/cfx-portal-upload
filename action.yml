name: 'Upload Escrow Asset'
description: 'Uploads an escrow asset to the cfx portal'
author: 'Lukas Leisten'

branding:
  icon: 'upload-cloud'
  color: 'gray-dark'

inputs:
  cookie:
    description: 'Your Forum Account Cookie'
    required: true
  makeZip:
    description:
      'Automatically zip the full repository to upload it (true or false)'
    required: false
    default: true
  assetName:
    description: 'The asset name to re-upload'
    required: false
  maxRetries:
    description: 'The maximum number of retries'
    required: false
    default: 3
  zipPath:
    description: 'The path to the zip file to upload'
    required: false
  version:
    description: 'Version string sent to the portal (overrides fxmanifest.lua in the zip)'
    required: false
  assetId:
    description: 'The asset id to re-upload'
    required: false
  beta:
    description: 'Whether to upload as a release candidate'
    required: false
  changelog:
    description: 'The changelog for the new version'
    required: false
  changelogFile:
    description: 'The path to a file containing the changelog'
    required: false
  deleteOlderVersions:
    description: 'Delete older versions of the asset after upload'
    required: false
    default: false
  skipUpload:
    description: 'Skip the upload and only go through the login process'
    required: false
  download:
    description: 'Download the asset after upload'
    required: false
    default: false
  downloadPath:
    description: 'The path where the downloaded asset will be saved'
    required: false
  chunkSize:
    description: 'The chunk size to split the file into'
    required: false
    default: 2097152

outputs:
  refreshed-cookie:
    description: 'Cookie _t forum.cfx.re après connexion au portal (à persister dans FORUM_COOKIE)'

runs:
  using: node24
  main: dist/index.mjs
